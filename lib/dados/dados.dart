import 'package:flutter/material.dart';
import 'gradient_dados.dart';

class MyDice extends StatelessWidget {
  const MyDice({super.key});

  @override
  Widget build(BuildContext context) {
    return const GradientContainer.purple();
  }
}
